# Mission Control Agent Rules

## Delivery

- Work on, commit, and push verified changes directly to `master` by default.
- Do not create a pull request unless Boyan explicitly asks for one.
- Keep commits coherent and leave `master` synchronized with `origin/master`.
